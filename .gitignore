# POKA-YOKE: Never push the unencrypted member portal source
# The source file is edited locally and encrypted via StatiCrypt
# Only the encrypted members.html gets pushed to the repo
members-source.html

# StatiCrypt intermediate output folder
encrypted/

# OS junk
.DS_Store
Thumbs.db
__pycache__/

# Generated palm card render output (deliverables are committed at the folder root)
print/palm-card-2026/source/build/
